[build-system]
requires = ["setuptools>=40.8.0", "wheel", 'cython>=0.21', 'numpy>=2.0', 'packaging>=14.1']
build-backend = "setuptools.build_meta:__legacy__"

[tool.cibuildwheel]
test-requires = ['packaging>=14.1']
